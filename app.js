var http = require("http");var fs = require("fs")var connect = require("connect");var logger = require("./logger");var logStream = fs.createWriteStream('./log.log', {'flags':'a', 'encoding': 'utf8', mode : 0666});var statStream = fs.createWriteStream('./stat.log', {'flags':'a', 'encoding': 'utf8', mode : 0666});var app = connect()    .use(connect.favicon())    .use(connect.logger({ stream : logStream }))    .use(logger({ stream : statStream }));http.createServer(app).listen(80);console.log("Server listening on port 80");